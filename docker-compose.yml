services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: studentos_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: studentos
      POSTGRES_PASSWORD: studentos_dev_password
      POSTGRES_DB: studentos_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U studentos -d studentos_db"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
